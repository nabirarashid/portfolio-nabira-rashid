---
title: the title of the piece, lowercase like the rest of the site
date: 2026-01-01
excerpt: optional. one or two sentences for the card. leave out to use the first paragraph.
cover: optional. a full url, or a path under public like /assets/writing/cover.jpg
substack: optional. https://nabirarashid.substack.com/p/the-same-piece
---

copy this file to `<slug>.md` in this folder (the filename becomes the url,
so `keep-something-real.md` is `/writing/keep-something-real`), fill in the
front matter above, and paste the article below it.

plain markdown works: paragraphs, **bold**, *italics*, [links](https://example.com),
lists, `code`, > quotes, and images with `![alt](url)`. headings with `##`.

files starting with an underscore, like this one, are ignored.
